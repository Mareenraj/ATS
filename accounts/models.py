"""
User accounts models.
Using Django's built-in User model.
"""
from django.contrib.auth.models import User

# We'll use Django's built-in User model
# Future enhancement: create custom user model or profile